readToPsql.pl:

	Read Bro log files into a postgresql database. Tables are 
        automatically generated according to the type and path
        values in the Bro logs.

	Usage: readToPsql.pl --db=[dbname] [--port=port] [logfiles]

readToInfobright.pl: 
	Very rough Infobright loader. Has hardcoded paths in it (search for /xc)
